# Acme Payments

Acme Store — Stripe Checkout + order fulfillment service.

Support keeps getting tickets: several customers say they were charged 2-3 times
for a single order. It passes our click-through test and returns 200 to Stripe,
so we can't see what's wrong.

The FetchSandbox MCP is configured (.mcp.json) — use it to investigate, fix, and prove the fix.
